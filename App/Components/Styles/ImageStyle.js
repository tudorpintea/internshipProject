import {StyleSheet} from 'react-native'; 
import { Metrics } from "../../Containers/Styles/themes"

export default StyleSheet.create({
    imageStyle: {
        width: Metrics.screenWidth,
        height: Metrics.screenHeight, 
        marginBottom: Metrics.screenHeight/2
    }
}) 